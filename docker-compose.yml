services:
  sonosync:
    build: .
    container_name: sonosync
    network_mode: "host"
    # No more volume mounts needed for runtime - the image is self-contained!
    environment:
      - LOG_LEVEL=INFO
      - DEBUG=
    restart: unless-stopped
